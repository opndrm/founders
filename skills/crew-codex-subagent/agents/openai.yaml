interface:
  display_name: "Crew Codex Sub Agent"
  short_description: "Delegate bounded work to a separate Codex worker"
  default_prompt: "Use Crew Codex Sub Agent to delegate this self-contained task to a Codex worker and return a reviewed result."
